# برومبت تطوير: توسيع تطبيق Exam Sorter ليصبح "مُنظّم الوسائط الذكي"

انسخ كل ما بين الخطين والصقه داخل شات Antigravity وأنت فاتح مجلد مشروع exam-sorter الحالي (المشروع موجود بالفعل، لا تبدأ من الصفر — طوّر فوق main.py وclassifier.py وfile_manager.py الموجودين).

---

## السياق
لدي تطبيق Kivy/KivyMD يعمل باسم Exam Sorter، يصنّف صور الاختبارات حسب المادة عبر Claude API + OCR محلي احتياطي. أريد توسيعه ليصبح **منظّم وسائط شامل للجوال بالكامل**، يعمل تلقائياً في الخلفية، يفحص كل الصور والفيديوهات في الذاكرة الداخلية والخارجية، ويرتبها في مجلدات حسب نوع المحتوى. هذا التطبيق شخصي لجوالي فقط (لن يُنشر على Google Play).

## تصنيف المحتوى المطلوب (بالترتيب — أول تطابق يفوز)
1. **صور اختبارات جامعية** → مجلد باسم المادة (أو اسم المدرس إن وُجد في نص الورقة) — يستخدم classifier.py الموجود حالياً.
2. **صوري أنا** → أي صورة يظهر فيها وجهي (سواء لوحدي أو مع آخرين) → مجلد "صوري".
3. **صور فيها أشخاص آخرون** (وجه بشري مكتشف وليس وجهي) → مجلد "صور الزملاء والإخوة".
4. **فيديوهات مضحكة** → مجلد "فيديوهات مضحكة".
5. **فيديوهات محاضرات/تعليمية** → مجلد "محاضرات وتعلم".
6. **أفلام ومسلسلات** → مجلد "أفلام ومسلسلات".
7. **أغاني وأناشيد** → مجلد "أغاني وأناشيد".
8. **أي شيء آخر لا يطابق ما سبق** (صورة أو فيديو) → مجلد "خارج التصنيف" يُنشأ تلقائياً في الذاكرة الخارجية.

كل هذه المجلدات تُنشأ تلقائياً إن لم تكن موجودة، داخل مسار أساسي واحد (مثلاً `/storage/emulated/0/MediaSorter/`).

## قرارات معتمدة (لا تُعِد سؤالي عنها)
- **التعرف على الوجه**: مرجع واحد فقط لصورتي أنا (سأرفعه من شاشة إعداد داخل التطبيق). أي وجه غير مطابق لوجهي يُصنَّف "زملاء وإخوة" مباشرة دون تمييز الأشخاص من بعضهم.
- **تصنيف الفيديو**: نظام مختلط — يحاول التصنيف محلياً أولاً (بدون إنترنت)، فإن توفر اتصال إنترنت يستخدم Claude API لتحسين الدقة عند الشك.
- **النقل**: **نقل فعلي (Move)** يحذف الملف من مكانه الأصلي بعد نقله — وليس نسخ. طبّق هذا بحذر شديد (تفاصيل السلامة أدناه).

## المكوّنات المطلوب بناؤها/تعديلها

### 1) `people_setup_screen.py` + شاشة KV جديدة
- شاشة إعداد أول مرة: المستخدم يلتقط أو يختار 3-5 صور واضحة لوجهه.
- استخرج من هذه الصور "بصمة وجه" (face embedding) وخزّنها محلياً (ملف JSON أو npy) في `user_data_dir`.
- زر لإعادة التدريب/تحديث الصور لاحقاً من الإعدادات.

### 2) `face_classifier.py` (جديد)
- دالة `detect_and_match_face(image_path) -> "me" | "other" | "none"`.
- استخدم OpenCV لاكتشاف الوجوه (Haar Cascade أو DNN face detector الجاهز في opencv-contrib) لأنه الأخف والأسهل تجميعاً لأندرويد عبر buildozer.
- لاستخراج بصمة الوجه للمقارنة: ابحث عن أخف مكتبة تعمل فعلياً ضمن python-for-android (رشّح: `onnxruntime` + نموذج embedding صغير مثل MobileFaceNet بصيغة ONNX يُضمَّن كملف asset، أو بديل أبسط دقته أقل إن تعذّر ذلك تقنياً). **مهم**: اختبر أولاً أن المكتبة المختارة تُبنى فعلياً عبر buildozer قبل بناء بقية الميزة عليها؛ إن فشل التجميع، أخبرني بالبديل المتاح بدل الاستمرار بحل لن يعمل على الجهاز.
- عتبة تشابه (threshold) قابلة للضبط لتقليل الأخطاء.

### 3) `video_classifier.py` (جديد)
دالة `classify_video(video_path) -> category` تعمل على طبقتين:
- **طبقة محلية (بدون إنترنت)**، بالترتيب:
  - اسم الملف ومسار المجلد المصدر (مثلاً فيديوهات من `WhatsApp/Media/WhatsApp Animated Gifs` أو أسماء ملفات تحتوي كلمات مثل "محاضرة"، "شرح"، "lecture" تلمح للتصنيف).
  - مدة الفيديو (moviepy أو ffprobe إن توفر) كإشارة مساعدة (أغنية عادة أقصر من فيلم مثلاً).
  - أخذ عينة صوت قصيرة (أول 10-15 ثانية) وتحويلها لنص عبر مكتبة تعرف كلام محلية خفيفة إن أمكن تشغيلها على أندرويد، ومطابقة كلمات مفتاحية (محاضرة/كورس/درس مقابل أغنية/كليب).
  - أخذ إطار أو إطارين (frame) من الفيديو وتمريرهما على نفس منطق كشف الوجوه (فيديو فيه وجوه كثيرة ومشاهد متغيرة يرجّح فيلم/مسلسل).
  - إن لم تحسم أي إشارة، أرجع "غير محدد محلياً".
- **طبقة Claude API (عند توفر إنترنت وعندما تكون النتيجة المحلية "غير محدد" أو بثقة منخفضة)**:
  - استخرج إطاراً واحداً representative من الفيديو (منتصف الفيديو تقريباً) وأرسله لـ Claude Vision مع البرومبت: "صنّف هذا المشهد من فيديو إلى واحدة فقط من: مضحك / محاضرة أو تعليمي / فيلم أو مسلسل / أغنية أو نشيد / غير ذلك. أجب بكلمة واحدة فقط."
  - هذه طبقة تحسين اختيارية فقط، والتطبيق يجب أن يعمل بالكامل بدونها إن انقطع الإنترنت.

### 4) `media_scanner.py` (جديد) — قلب الميزة
- دالة `scan_storage_roots() -> list[Path]`: أعد كل الجذور القابلة للفحص (الذاكرة الداخلية المشتركة `/storage/emulated/0`، وأي بطاقة SD خارجية تظهر عادة في `/storage/XXXX-XXXX` على أندرويد).
- دالة `find_unsorted_media(roots) -> list[Path]`: امشِ recursively على الصور (`.jpg/.jpeg/.png/.webp`) والفيديوهات (`.mp4/.mkv/.3gp/.mov`)، **باستثناء**:
  - مجلد التطبيق الأساسي `MediaSorter/` نفسه (لتفادي إعادة تصنيف ما صُنّف فعلاً).
  - `Android/data`, `Android/obb` (محمية على أندرويد 11+، والوصول إليها ممنوع/عديم الفائدة).
  - أي ملف تم فحصه سابقاً ولم يتغيّر (احتفظ بسجل بسيط: مسار + hash أو mtime، في ملف/قاعدة بيانات sqlite محلية صغيرة، لتفادي إعادة فحص نفس الملفات في كل تشغيل).
- دالة `process_one_file(path)`: تحدد نوع الملف (صورة/فيديو) وتمرره على منطق التصنيف المناسب بالترتيب المذكور أعلاه، ثم تستدعي `file_manager.move_to_category(...)`.
- شغّل الفحص على دفعات صغيرة (batch) مع توقف بسيط بين كل عملية لتفادي تجميد الواجهة (ANR) واستهلاك بطارية مفرط.

### 5) تحديث `file_manager.py`
- أضف `move_to_category(src_path, category_name) -> Path`: تنشئ المجلد إن لم يوجد، **تنقل** (`shutil.move`) وليس تنسخ، وتتعامل مع تعارض الأسماء (إضافة رقم تسلسلي بدل الكتابة فوق ملف موجود).
- **حماية أساسية قبل أي حذف**: تحقق أن النقل نجح فعلياً (الملف الجديد موجود وبنفس الحجم) قبل اعتبار العملية القديمة "منقولة"؛ إن فشل النسخ الفعلي لا تحذف الأصل أبداً.
- سجلّ كل عملية نقل (المسار القديم → الجديد + الوقت) في ملف log بسيط، حتى يقدر المستخدم يتراجع يدوياً لو صُنّف شيء غلط.

### 6) الخدمة الخلفية (Background Service) — المراقبة التلقائية اللحظية
هذا الجزء الأكثر تعقيداً تقنياً على أندرويد، نفّذه بعناية:
- أنشئ `service/media_watcher_service.py` كخدمة Android منفصلة عبر آلية `services` في python-for-android/buildozer (وليس داخل نفس عملية الواجهة الرئيسية).
- داخل الخدمة: استخدم `watchdog` (إن توفّر عمله على أندرويد) أو بديل مبني يدوياً بفحص دوري كل بضع ثوانٍ لمجلدات الالتقاط الشائعة (`DCIM/Camera`, `Download`, `WhatsApp/Media/...`) بحثاً عن ملفات جديدة (بالاعتماد على وقت التعديل)، فور ظهور ملف جديد مكتمل الكتابة (تحقق من ثبات حجم الملف بين قراءتين متتاليتين قبل معالجته، حتى لا تعالج ملفاً لا يزال قيد التنزيل/التصوير) يُستدعى `media_scanner.process_one_file`.
- اجعلها Foreground Service مع إشعار دائم بسيط ("منظّم الوسائط يعمل في الخلفية") لأن أندرويد يقتل الخدمات الخلفية العادية.
- أضف زر تشغيل/إيقاف للخدمة من الشاشة الرئيسية، وابدأ الخدمة تلقائياً عند فتح التطبيق أول مرة بعد موافقة المستخدم.

### 7) الصلاحيات و`buildozer.spec`
- أضف صلاحية `MANAGE_EXTERNAL_STORAGE` (صلاحية خاصة تحتاج توجيه المستخدم لشاشة إعدادات أندرويد يدوياً للموافقة عليها، أضف Intent يفتحها مباشرة من التطبيق).
- أضف `FOREGROUND_SERVICE` و`POST_NOTIFICATIONS` (لأندرويد 13+).
- أضف المكتبات الجديدة إلى `requirements` في buildozer.spec (opencv-python أو opencv-python-headless، onnxruntime إن أمكن تجميعها، numpy) — **تحقق أولاً من توافقها الفعلي مع python-for-android** وأخبرني فوراً بأي مكتبة يتعذر تجميعها بدل المتابعة بصمت.

### 8) شاشة الإعدادات
- عرض حالة الخدمة (تعمل/متوقفة)، عدد الملفات المصنّفة، زر "امسح الآن يدوياً"، رابط "سجلّ عمليات النقل الأخيرة" (لمراجعة/التراجع).

## خطة العمل المطلوبة منك (نفّذ بالترتيب وأخبرني بعد كل مرحلة)
1. اقرأ الكود الحالي بالكامل (main.py, classifier.py, file_manager.py, screens/*, utils/*) وأكّد فهمك للبنية قبل التعديل.
2. ابنِ `face_classifier.py` وشاشة `people_setup_screen.py`، واختبرهما على صور ثابتة (بدون خدمة خلفية بعد).
3. ابنِ `video_classifier.py` (الطبقة المحلية أولاً، ثم طبقة Claude API).
4. ابنِ `media_scanner.py` و`move_to_category` في file_manager.py، واختبر على مجلد تجريبي صغير مُنشأ يدوياً (ليس على كامل الذاكرة الحقيقية بعد).
5. ابنِ الخدمة الخلفية الخلفية وحدّث buildozer.spec والصلاحيات.
6. حدّث الشاشة الرئيسية وأضف شاشة الإعدادات.
7. اكتب لي تعليمات اختبار واضحة: كيف أختبر على المحاكي أولاً بمجلد وهمي قبل تجربته على جوالي الحقيقي بملفاتي الفعلية (هذا مهم جداً لأن النقل حذف فعلي).

ابدأ بالخطوة 1 الآن.
---
