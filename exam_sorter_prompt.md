# برومبت بناء تطبيق: مصنّف صور الاختبارات الجامعية

انسخ كل ما بين الخطين والصقه في شات المساعد البرمجي داخل VS Code.

---

أريد منك بناء تطبيق موبايل (أندرويد بشكل أساسي، مع إمكانية العمل على iOS لاحقاً) بلغة Python بالكامل، باستخدام Kivy و KivyMD كإطار عمل للواجهة، و Buildozer لتحويله لملف APK.

## وظيفة التطبيق
التطبيق يساعد طالب جامعي على تنظيم صور أوراق الاختبارات تلقائياً:
1. يفتح المستخدم التطبيق ويختار صورة/صور اختبار (من الكاميرا أو من المعرض).
2. يقوم التطبيق بإرسال الصورة لنموذج ذكاء اصطناعي (Vision Model) ليتعرّف على اسم المادة الدراسية المكتوب في ورقة الاختبار (مثل: رياضيات، فيزياء، لغة إنجليزية... إلخ).
3. يتحقق التطبيق: هل يوجد مجلد بهذا الاسم داخل مساحة تخزين مخصصة للتطبيق (مثلاً: `ExamSorter/اسم_المادة/`)؟ إن لم يوجد، يُنشئه تلقائياً.
4. يقوم بنسخ/نقل الصورة إلى ذلك المجلد، وإعادة تسميتها بشكل منظم (مثال: `اسم_المادة_2026-09-24_01.jpg`).
5. تظهر للمستخدم رسالة تأكيد باسم المادة والمجلد الذي وضعت فيه الصورة، مع إمكانية التصحيح اليدوي إن أخطأ الذكاء الاصطناعي في التصنيف (قائمة منسدلة لاختيار/تعديل اسم المادة قبل الحفظ النهائي).
6. شاشة رئيسية تعرض قائمة المواد (المجلدات) الموجودة، وعدد الصور في كل مادة، مع إمكانية فتح أي مجلد ومعاينة الصور بداخله.

## المتطلبات التقنية

### 1) الواجهة (UI)
- استخدم KivyMD (Material Design) لواجهة عصرية وسهلة.
- الشاشات المطلوبة:
  - `HomeScreen`: قائمة المواد + زر عائم (FAB) لإضافة صورة اختبار جديدة.
  - `CaptureScreen`: اختيار صورة من الكاميرا أو المعرض.
  - `ClassifyingScreen`: شاشة تحميل بسيطة أثناء انتظار رد الذكاء الاصطناعي.
  - `ConfirmScreen`: عرض نتيجة التصنيف مع إمكانية تعديل اسم المادة يدوياً قبل الحفظ.
  - `SubjectDetailScreen`: عرض صور مادة معينة في شبكة (Grid).
- ادعم اللغة العربية في الواجهة (اتجاه ونصوص).

### 2) منطق التصنيف بالذكاء الاصطناعي
أنشئ ملف `classifier.py` يحتوي على دالة `classify_exam_image(image_path) -> str` تُرجع اسم المادة.

اعمل الخيار التالي كافتراضي (الأدق):
- استخدم Anthropic API (Claude) عبر endpoint: `https://api.anthropic.com/v1/messages`
- أرسل الصورة (base64) مع تعليمة واضحة للنموذج: "هذه صورة ورقة اختبار جامعي. اقرأ العنوان/الترويسة وحدد اسم المادة الدراسية فقط، أجب باسم المادة فقط بدون أي نص إضافي، بالعربية إن كانت مكتوبة بالعربية."
- اجعل مفتاح الـ API يُقرأ من ملف إعدادات `.env` أو من حقل إعدادات داخل التطبيق (لا تكتبه مباشرة في الكود).
- أضف معالجة أخطاء (لا يوجد إنترنت / فشل الطلب) بحيث يطلب من المستخدم إدخال اسم المادة يدوياً كبديل.

أضف أيضاً كخيار ثانٍ (تعليق/إعداد يمكن تفعيله) وضع بنية جاهزة لاستخدام OCR محلي بديل (pytesseract) لاستخراج النص من الصورة، مع دالة بسيطة لمطابقة كلمات مفتاحية (مثل "رياضيات"، "كيمياء"...) في حال أراد المستخدم حلاً يعمل بدون إنترنت.

### 3) إدارة الملفات
أنشئ ملف `file_manager.py` يحتوي على:
- `ensure_subject_folder(base_path, subject_name) -> Path`: ينشئ المجلد إن لم يكن موجوداً (مع تنظيف الاسم من رموز غير مسموحة لأنظمة الملفات).
- `save_image_to_subject(image_path, subject_name)`: ينسخ الصورة للمجلد المناسب بتسمية منظمة تتضمن التاريخ ورقم تسلسلي.
- `list_subjects(base_path)`: يرجع قائمة المواد وعدد الصور بكل مادة، لعرضها في `HomeScreen`.
- استخدم مسار تخزين مناسب لأندرويد (مساحة التطبيق الخاصة عبر `android.storage` أو `plyer.storagepath`، مع طلب صلاحيات القراءة/الكتابة اللازمة عبر `plyer` أو `pyjnius` عند الحاجة).

### 4) هيكل المشروع المطلوب
```
exam_sorter/
├── main.py
├── classifier.py
├── file_manager.py
├── screens/
│   ├── home_screen.py
│   ├── capture_screen.py
│   ├── classifying_screen.py
│   ├── confirm_screen.py
│   └── subject_detail_screen.py
├── kv/
│   └── (ملفات .kv لكل شاشة إن فضّلت الفصل)
├── assets/
├── .env.example
├── requirements.txt
└── buildozer.spec
```

### 5) المكتبات المطلوبة في requirements.txt
kivy, kivymd, pillow, requests, plyer, python-dotenv (وpytesseract إن فُعّل خيار OCR المحلي)

### 6) buildozer.spec
جهّز ملف buildozer.spec بالصلاحيات اللازمة لأندرويد: `CAMERA`, `READ_EXTERNAL_STORAGE`, `WRITE_EXTERNAL_STORAGE`, `INTERNET`.

## طريقة العمل المطلوبة منك
1. ابدأ بإنشاء هيكل المشروع والملفات الفارغة.
2. اكتب `file_manager.py` وشرح موجز لكل دالة.
3. اكتب `classifier.py` بالكامل مع تعليقات توضح أين أضع مفتاح API.
4. اكتب `main.py` وكل الشاشات في `screens/` مع ملفات `.kv` المرتبطة بها.
5. اكتب `requirements.txt` و `buildozer.spec` و `.env.example`.
6. في النهاية، اشرح لي بالتفصيل خطوات التشغيل على المحاكي، وخطوات البناء النهائي لملف APK عبر buildozer، خطوة بخطوة.

ابدأ الآن بالخطوة 1.
---
